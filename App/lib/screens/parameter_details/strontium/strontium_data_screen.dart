import 'package:flutter/material.dart';

class StrontiumDataScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Data for Strontium'),
    );
  }
}

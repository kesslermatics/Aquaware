import 'package:flutter/material.dart';

class IodineAlertsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Alerts for Iodine'),
    );
  }
}

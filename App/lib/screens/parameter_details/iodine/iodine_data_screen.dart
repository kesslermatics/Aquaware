import 'package:flutter/material.dart';

class IodineDataScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Data for Iodine'),
    );
  }
}

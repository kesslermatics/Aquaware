import 'package:flutter/material.dart';

class SuspendedSolidsKnowledgeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Knowledge about Suspended Solids'),
    );
  }
}

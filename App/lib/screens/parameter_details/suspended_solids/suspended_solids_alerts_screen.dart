import 'package:flutter/material.dart';

class SuspendedSolidsAlertsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Alerts for Suspended Solids'),
    );
  }
}

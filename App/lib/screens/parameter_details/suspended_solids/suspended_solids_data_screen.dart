import 'package:flutter/material.dart';

class SuspendedSolidsDataScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Data for Suspended Solids'),
    );
  }
}

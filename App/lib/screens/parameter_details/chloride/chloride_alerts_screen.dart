import 'package:flutter/material.dart';

class ChlorideAlertsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Alerts for Chloride'),
    );
  }
}

import 'package:flutter/material.dart';

class AlkalinityKnowledgeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Knowledge about Alkalinity'),
    );
  }
}

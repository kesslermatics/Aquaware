import 'package:flutter/material.dart';

class AlkalinityDataScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Data for Alkalinity'),
    );
  }
}

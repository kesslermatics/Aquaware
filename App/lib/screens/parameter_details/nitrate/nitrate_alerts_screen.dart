import 'package:flutter/material.dart';

class NitrateAlertsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Alerts for Nitrate'),
    );
  }
}

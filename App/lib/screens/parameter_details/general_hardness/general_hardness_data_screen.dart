import 'package:flutter/material.dart';

class GeneralHardnessDataScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Data for General Hardness'),
    );
  }
}

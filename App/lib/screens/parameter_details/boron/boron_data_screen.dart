import 'package:flutter/material.dart';

class BoronDataScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Data for Boron'),
    );
  }
}

import 'package:flutter/material.dart';

class BoronKnowledgeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Knowledge about Boron'),
    );
  }
}

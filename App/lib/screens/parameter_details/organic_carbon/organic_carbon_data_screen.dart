import 'package:flutter/material.dart';

class OrganicCarbonDataScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Data for Organic Carbon'),
    );
  }
}

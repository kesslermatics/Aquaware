import 'package:flutter/material.dart';

class SilicaAlertsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Alerts for Silica'),
    );
  }
}

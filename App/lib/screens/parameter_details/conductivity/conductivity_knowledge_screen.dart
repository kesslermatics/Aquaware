import 'package:flutter/material.dart';

class ConductivityKnowledgeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Knowledge about Conductivity'),
    );
  }
}

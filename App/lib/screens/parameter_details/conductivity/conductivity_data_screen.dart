import 'package:flutter/material.dart';

class ConductivityDataScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Data for Conductivity'),
    );
  }
}

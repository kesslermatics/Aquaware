import 'package:flutter/material.dart';

class SalinityKnowledgeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Knowledge about Salinity'),
    );
  }
}

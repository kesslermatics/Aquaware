import 'package:flutter/material.dart';

class SalinityDataScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Data for Salinity'),
    );
  }
}

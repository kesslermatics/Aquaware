import 'package:flutter/material.dart';

class MagnesiumDataScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Data for Magnesium'),
    );
  }
}

import 'package:flutter/material.dart';

class TurbidityKnowledgeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Knowledge about Turbidity'),
    );
  }
}

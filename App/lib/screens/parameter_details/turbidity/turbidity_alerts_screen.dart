import 'package:flutter/material.dart';

class TurbidityAlertsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Alerts for Turbidity'),
    );
  }
}

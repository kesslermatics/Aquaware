import 'package:flutter/material.dart';

class IronKnowledgeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Knowledge about Iron'),
    );
  }
}

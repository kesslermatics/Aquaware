import 'package:flutter/material.dart';

class RedoxPotentialAlertsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Alerts for Redox Potential'),
    );
  }
}

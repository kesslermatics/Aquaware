import 'package:flutter/material.dart';

class PHDataScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Data for PH'),
    );
  }
}

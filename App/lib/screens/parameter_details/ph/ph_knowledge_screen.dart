import 'package:flutter/material.dart';

class PHKnowledgeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Knowledge about PH'),
    );
  }
}

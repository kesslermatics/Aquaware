import 'package:flutter/material.dart';

class PHAlertsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Alerts for PH'),
    );
  }
}

import 'package:flutter/material.dart';

class ChlorineAlertsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Alerts for Chlorine'),
    );
  }
}

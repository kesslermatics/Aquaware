import 'package:flutter/material.dart';

class PhosphateDataScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Data for Phosphate'),
    );
  }
}

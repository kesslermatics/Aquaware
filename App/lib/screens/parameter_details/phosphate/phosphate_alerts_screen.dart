import 'package:flutter/material.dart';

class PhosphateAlertsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Alerts for Phosphate'),
    );
  }
}

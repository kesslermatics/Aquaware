import 'package:flutter/material.dart';

class CopperAlertsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Alerts for Copper'),
    );
  }
}

import 'package:flutter/material.dart';

class CopperDataScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Data for Copper'),
    );
  }
}

import 'package:flutter/material.dart';

class TDSDataScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Data for TDS'),
    );
  }
}

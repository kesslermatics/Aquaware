import 'package:flutter/material.dart';

class TDSKnowledgeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Knowledge about TDS'),
    );
  }
}

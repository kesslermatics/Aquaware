import 'package:flutter/material.dart';

class TDSAlertsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Alerts for TDS'),
    );
  }
}

import 'package:flutter/material.dart';

class SulfateAlertsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Alerts for Sulfate'),
    );
  }
}

import 'package:flutter/material.dart';

class FluorideDataScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Data for Fluoride'),
    );
  }
}

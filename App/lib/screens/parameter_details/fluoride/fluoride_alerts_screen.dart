import 'package:flutter/material.dart';

class FluorideAlertsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Alerts for Fluoride'),
    );
  }
}

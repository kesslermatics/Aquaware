import 'package:flutter/material.dart';

class BromineAlertsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Alerts for Bromine'),
    );
  }
}

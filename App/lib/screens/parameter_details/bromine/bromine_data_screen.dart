import 'package:flutter/material.dart';

class BromineDataScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Data for Bromine'),
    );
  }
}

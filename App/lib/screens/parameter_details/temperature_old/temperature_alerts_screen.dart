import 'package:flutter/material.dart';

class TemperatureAlertsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Alerts for Temperature'),
    );
  }
}

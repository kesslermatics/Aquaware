import 'package:flutter/material.dart';

class AmmoniaAlertsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Alerts for Ammonia'),
    );
  }
}

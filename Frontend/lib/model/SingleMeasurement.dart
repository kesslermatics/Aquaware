class SingleMeasurement {
  double temperature;
  double tds;
  double ph;
  String createdAt;
  int entryID;

  SingleMeasurement(
      this.temperature, this.tds, this.ph, this.createdAt, this.entryID);
}
